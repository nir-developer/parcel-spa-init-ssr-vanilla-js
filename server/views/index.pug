doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title title
    body 
        if name
            p welcome, #{name}
            ul
                each task in tasks
                    li= task.title
        else 
            p Please login or register
            //ADD LOGIN /REGISTER FORMS OR LINKS 
        